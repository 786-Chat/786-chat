# Regeneration quality checks

When a user asks to regenerate, rebuild, redesign or replace a project completely, the admin builder removes prior project code from the model context and creates a clean design seed.

Generated results are rejected when they contain admin/editor code, generic repeated template slogans, or industry-inappropriate sections. One stricter automatic retry is attempted; invalid files are not saved.
