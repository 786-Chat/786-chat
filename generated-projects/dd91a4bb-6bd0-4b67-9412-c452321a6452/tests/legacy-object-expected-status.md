Expected live result after deployment: the legacy object request must not emit a `PRIVATE_OBJECT_DIR not set` runtime error. If the binary is absent from Vercel Blob, HTTP 404 is correct and bounded.
